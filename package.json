{
  "name": "gl-api",
  "version": "1.0.0",
  "description": "API for challenge by Grupo Lagos",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch --experimental-sqlite .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fperez",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
